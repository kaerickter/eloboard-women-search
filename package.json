{
  "name": "eloboard-women-search",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python -m pip install --user -U yt-dlp || python3 -m pip install --user -U yt-dlp || echo yt-dlp install skipped",
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  },
  "engines": {
    "node": ">=18"
  }
}
